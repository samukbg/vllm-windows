__pycache__/
*.pyc
*.pyo
*.egg-info/

# Logs and per-run artefacts
logs/
runs.tsv
launcher.out
*.log

# User model weights (never check these in)
models/

# Local venv
venv/
.venv/

# Build outputs (portable launcher zip + intermediates)
dist/
.build_launcher/
.build_launcher-*/
.build_wheels/
launcher/python/

# TUI development helpers (per-dev, not shipped)
view_app.py
test_dashboard_arch.py
.tui_screens/

# Maintainer-only release runbook
RELEASING.md

# Local handoff notes (per-session, not pushed)
HANDOFF.md

# Tokenizer/config backups produced by patch_tokenizer.py
tokenizer_config.json.bak
*.upstream.bak

# OS / editor cruft
Thumbs.db
.DS_Store
.vscode/
.idea/

# Local-only notes (drafts, scratchpads, never pushed).
# Covers per-agent context files dropped by various coding tools.
_local/
AGENT_NOTES.md
AGENTS.md
CLAUDE.md
GEMINI.md
.cursorrules
.cursor/
.aider*
.windsurfrules

# Cursor conversation exports (/export drops these at repo root)
20[0-9][0-9]-[0-1][0-9]-[0-3][0-9]-*.txt

# Bundled vLLM wheel, built separately, downloaded at release time
wheels/vllm*.whl
